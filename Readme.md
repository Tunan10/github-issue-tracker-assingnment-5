1.What is the difference between var, let, and const?

=> var declarations are hoisted to the top of their scope, meaning the variable
exists before the declaration, but its value is undefined until assignment.

cosnt variable declaration keywords is fixxed value variable declaration
keywords. we cannot change the value of it. it will remain constant.

and let variable declaration keywords we can change the value of it.

2.What is the spread operator (...)?

=> The ... spreads all items out.

3.What is the difference between map(), filter(), and forEach()?

=> map() makes a new array by changing each item.

filter() makes a new array with only items that pass a test.

forEach() runs a function on each item, but doesn’t return anything.

4.What is an arrow function? =>A shorter way to write a function using =>

5.What are template literals?

=>Strings wrapped in backticks (`) that allow variables and expressions inside
${}.
